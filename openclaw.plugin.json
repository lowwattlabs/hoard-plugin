{
  "id": "hoard",
  "name": "HOARD",
  "description": "Durable agent memory that survives session resets. Structured markdown persistence with provenance, auto-expiry, and consolidation. Delete is soft by default \u2014 moves to trash, not permanent removal. Permanent deletion requires explicit confirmation.",
  "version": "2.1.1",
  "configSchema": {
    "type": "object",
    "properties": {
      "memoryDir": {
        "type": "string",
        "description": "Absolute path to the memory directory. Defaults to ./memory. Must be a dedicated storage folder \u2014 not home, root, or system directories."
      },
      "autoExpire": {
        "type": "boolean",
        "description": "Enable automatic expiry of old entries (default: true)"
      },
      "consolidateOnStartup": {
        "type": "boolean",
        "description": "Run consolidation on plugin startup (default: false)"
      }
    }
  },
  "activation": {
    "onStartup": false
  },
  "contracts": {
    "tools": [
      "hoard"
    ]
  }
}